import styled from "styled-components";

const ContaineDond = styled.div`
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(100%, 1fr));
  justify-items: center;
  img {
    width: 100%;
  }
`;

export { ContaineDond };
